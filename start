#!/usr/bin/env bash

# Install pip3 Packages
pip3 install -U -r requirements.txt

if [ "$1" = "first-time" ]
then python3 -c "from Server.Database import create_tables;create_tables()"
fi

# Start Server
gunicorn main:app
